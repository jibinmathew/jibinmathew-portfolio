# .gitlab-ci.yml
# Configuration for deploying static website to GitLab Pages

image: alpine:latest

pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r index.html css js images public/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
